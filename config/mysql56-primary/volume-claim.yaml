apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql56-primary-pvc
  labels:
    app: dev
spec:
  storageClassName: mysql56-primary-volume
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
